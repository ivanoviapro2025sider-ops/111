# AGENTS.md

## Cursor Cloud specific instructions

This repository is currently an empty scaffold (only a `README.md` with placeholder content on `main`). There are no applications, services, dependencies, build systems, or tests to run.

### Repository state
- **Main branch**: Single `README.md` file, no source code.
- **Other branches**: Contain experimental/unmerged projects (e.g., `dep-analyzer-*`, `kimi-swarm-*`). These are independent and not part of the main branch.

### Development notes
- No package manager, build tool, or language runtime is required.
- No lint, test, or build commands exist.
- No services need to be started.
- When source code is added to `main`, this file should be updated with relevant setup, build, test, and run instructions.
